# 0000. 결정을 서술하는 평서문을 제목으로 쓴다.

## Status

`Proposed` · `Accepted` · `Superseded by [ADR NNNN](NNNN-슬러그.md)` 셋 중 하나를 쓴다.
상태 값은 이 셋뿐이다. `Deprecated` · `Rejected` 는 쓰지 않는다 —
탈락한 대안은 `Alternatives` 가 담고, 대체 없이 죽는 결정은 "무엇을 제거한다" 는 새 ADR 이 담는다.

**줄의 첫 토큰이 반드시 이 셋 중 하나여야 한다.** 뒤에 오는 것은 전부 부연이며
괄호나 `—` 로 잇는다. 인덱스와 스킬이 상태를 읽을 때 첫 토큰만 보면 되게 하기 위한 규칙이다.

```text
Accepted
Accepted (MVP 한정)
Proposed (부분 미결 — 서명 키 회전 절차 미정)
Superseded by [ADR 0012](0012-슬러그.md)
```

**부분 대체는 `Accepted` 로 시작한다.** 문서 대부분이 여전히 구속력을 가지는데 `Superseded` 로
시작하면 읽는 쪽이 통째로 버린다. 무엇이 대체됐고 무엇이 유효한지를 같은 줄에 적는다.

```text
Accepted — 부분 대체: [ADR 0006](0006-슬러그.md) 이 재배선 기본값을 대체한다. 나머지 결정은 유효하다.
```

**전체 대체만 `Superseded by` 로 시작한다.** 그때는 대체하는 ADR 의 Status 도 같은 커밋에서 고친다.

## Context

이 결정이 필요해진 배경을 쓴다. 어떤 제약이 이미 존재하는지, 무엇이 문제인지, 무엇을 전제로 하는지.
**결정하지 않으면 무슨 일이 일어나는가**까지 적는다. 코드나 문서를 근거로 삼았다면 파일 경로와 절 번호를 함께 남긴다.

여기서 결정을 미리 말하지 않는다. 사실만 적는다.

## Decision

무엇을 하기로 했는지 평서문 현재형으로 단정해 쓴다.
여러 항목이면 목록으로 나누되, 각 항목이 검증 가능한 문장이 되게 한다.

## Alternatives

### A. 대안 이름
- 장점:
- 단점:
- 탈락 이유:

### B. 대안 이름
- 장점:
- 단점:
- 탈락 이유:

## Consequences/Tradeoffs

### Positive
- 이 결정으로 얻는 것.

### Negative
- 이 결정으로 감수하는 것. 완화책이 있으면 하위 항목으로 붙인다.
- **여기가 비어 있으면 대개 검토가 덜 된 것이다.**

## Follow-up

- 이 ADR 이 미뤄둔 것, 후속 티켓으로 넘긴 것, 다시 검토해야 할 조건.
- 완료된 항목은 지우지 말고 **`**완료** —`** 를 앞에 붙여 남긴다. 무엇이 닫혔는지가 기록이다.
  취소선(`~~…~~`)은 쓰지 않는다 — 무엇이 왜 닫혔는지가 남지 않는다.

---

**작성 규칙**

- 섹션 순서는 위와 같다.
- **작성일은 문서에 적지 않는다.** `git log --follow <파일>` 로 확인한다.
- 파일명은 `NNNN-한국어-슬러그.md` 다. 이 레포의 관례이며 다른 레포와 통일하지 않는다.
- 다른 레포의 코드·계약에 걸리는 결정은 이 레포가 아니라 **문서 허브(`soma-376/docs`)의 `adr/`** 에 쓴다.
  판단이 애매하면 이렇게 묻는다 — *"이 결정을 뒤집으려면 몇 개 레포의 PR 이 필요한가."* 둘 이상이면 허브다.
- `contracts/*.schema.json` 과 `internal/contract/` 는 계약의 기계 판독 원본이다.
  이 둘을 바꾸는 결정은 거의 항상 허브 ADR 이다.
